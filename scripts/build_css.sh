#!/bin/bash
# Placeholder script for Tailwind CSS production build
# In production, we would use the Tailwind CLI to scan files and generate minimal CSS.

echo "Building Tailwind CSS for production..."
# npx tailwindcss -i ./static/css/input.css -o ./static/css/tailwind.min.css --minify
echo "Build complete (simulated)."
