# GeBankSaint

Integración Automatizada de Cobranzas entre Saint y Insytech con Soporte Multimoneda y Reportes de Comisiones.

## Setup

1. Install dependencies: `uv pip install -r requirements/dev.txt`
2. Run server: `uv run uvicorn app.main:app --reload`
